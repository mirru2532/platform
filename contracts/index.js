const distribution = require("./migrations/distro.json")
const contracts = require("./contracts.json")
const rewards = require("./vlcvx_token_rewards.json")

module.exports = { distribution, contracts, rewards }